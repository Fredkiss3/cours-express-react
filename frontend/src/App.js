import './App.css';
import { Button } from "./components/Button";

function App() {
  return (
    <>
      <Button value={'hello'} />
      <Button value={'bonjour'} />
      <Button value={'hola'} />
      <Button value={'buon jiorno'} />
    </>
  );
}

export default App;
